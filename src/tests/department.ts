import { describe, it, expect } from 'vitest';

describe('Connect Preact', () => {
  it('It should work well responsively for stateRef value.', () => {
    expect(true).toBe(true);
  });
  it('It should work well responsively for stateRef value2', () => {
    expect(true).toBe(true);
  });
});
